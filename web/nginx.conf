server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota que não é um arquivo real cai no index.html,
    # senão /caixa ou /estoque dão 404 ao recarregar a página.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
